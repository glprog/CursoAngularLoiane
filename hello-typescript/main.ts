var minhaVar = 'minha variavel';

function minhaFunc(x, y) {
    return x + y;
}

class Pessoa {
    nome : string;
}

let p = new Pessoa();
let numeros = [1, 2, 3];
numeros.map((x) => x * 2);

class Matematica {
    soma(x, y){
        return x + y;
    }
}